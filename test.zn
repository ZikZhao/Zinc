class A {
    let x: i32;
    init(x: i32) { return Self{ x: x }; }
    fn foo(self: &mut Self) { self.x += 1; }
}

enum B {
    C,
    D,
}

fn main(args: [strview]) {
    let mut a = A(1);
    a.foo();
}

