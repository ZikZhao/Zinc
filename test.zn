class A{
    let a: i32;
    init() {
        return Self{};
    }
    fn foo(self: &mut Self) {
        self.a = 5;
    }
}

fn main(args: [strview]) {
    let c: *u32;
    (c as *void);
    let mut a: A = A();
    a.foo();
}
