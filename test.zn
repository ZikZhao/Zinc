fn main(args: [strview]) {
    let a = Window();
    a.get_pixel_buffer();
}

class Window {
    init() {
        return Self{};
    }
    fn get_pixel_buffer(self: &Self) -> &std.vector<u32> {
        
    }
}