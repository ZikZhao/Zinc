class A {
    let x: i32;
    init(x: i32) { return Self{ x: x }; }
    fn foo(self: &Self) {}
}

enum B {
    C,
    D,
}

type E = std.pair<*A, i32>;

fn main(args: [strview]) {
    let mut a: std.pair<*E, i32>;
}

