class A{
    let a: i32;
    init(a: i32, b: i32) {
        return Self{a: a};
    }
    fn foo(self: &mut Self) {
        self.a = 5;
        Self.bar(5);
    }
    static fn bar(a: i32) -> i32 {
        return 1;
    }
}

fn main(args: [strview]) {
    A.bar(5);
}
