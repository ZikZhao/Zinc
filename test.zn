type A = i32 | f32;

fn main(args: [strview]) {
    std.cout << "Hello, world!";
    let mut x: A = 42;
    match (x) {
        a: i32 => { std.cout << a; }
        _ => { std.cout << "It's something else!"; }
    }
}

