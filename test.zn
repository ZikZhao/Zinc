class A{
    let a: i32;
    init(a: i32, b: i32) {
        return Self{a: a};
    }
    fn foo(self: &mut Self) {
        self.a = 5;
    }
}

fn main(args: [strview]) {
    let mut a: std.vector<i32>;
    a.emplace_back(1);
}
