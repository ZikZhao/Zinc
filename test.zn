let a: i8 = 127;

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn sub(a: i32, b: i32) -> i32 { return a - b; }

fn sub(a: f64, b: f64) -> f64 { return a - b; }

let f: (i32, i32) -> i32 = add;

let g = sub;

namespace DataCenter {
    let a = 1;
    let b = 1.0;
};

type Node = {
    value: i32,
    next: *Node
};

class Base {
    let data: i32;
    let data2: f64;
    init(self: &mut Self, value: i32) {
        self.data = value;
    }
    fn add1(self: &Self, other: i32) -> i32 {
        return self.data + other;
    }
};

class Derived {
    let data3: bool;
    fn sub1(self: &Self) -> i32 {
        if (self.data3) {
            return 1;
        } else {
            return 0;
        }
    }
};

fn main() -> i32 {
    f(2, 3);
    g(5, 4);
    g(3.0, 2.0);
    let a = DataCenter.a;
    let b = Node{ value: 1, next: nullptr };
    let c = b.value;
    let d = Base();
    let e = d.add(1);
}
