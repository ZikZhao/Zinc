class A{
    let a: i32;
    init() {
        return Self{};
    }
    fn foo(self: &mut Self) {
        self.a = 5;
    }
}

fn main(args: [strview]) {
    let a: *i32;
    let b = std.next(a, 1 as isize);
}
